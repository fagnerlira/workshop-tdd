package modulo2.exercicio.tdd.sessoe;

import java.util.List;

import org.junit.Assert;
import org.junit.Test;

import modulo2.exercicio.tdd.sessoes.GeradorDeSessoes;

public class GeradorDeSessoesTeste {

	@Test
	public void TesteUmDia() {

		GeradorDeSessoes geradorDeSessoes = new GeradorDeSessoes();

		//List<Integer> lista = geradorDeSessoes.gera(1, 1, 1);
		//int[] lista = geradorDeSessoes.gera(1, 1, 1);

		//lista.size();
		//lista.get(0);

		//Assert.assertEquals(2, lista[1]);
		//Assert.assertEquals(1, lista.size());
		//Assert.assertEquals(1, lista.get(0).intValue());

	}

}
